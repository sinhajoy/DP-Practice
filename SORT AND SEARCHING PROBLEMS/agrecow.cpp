#include<bits/stdc++.h>
using namespace std;

bool check(vector<int> a,int n,int )

int result(vector<int> a,int l,int r,int cow)
{
    sort(a.begin(), a.end());
    m

}

int main()
{
    int n;
    cin>>n;
    while(n--)
    {
        int len,cow;
        cin >> len>>cow;
        vector<int> stall;
        for(int i=0;i<len;i++){
            int x;
            cin >> x;
            stall.push_back(x);
        }
    }
}